# Question 4 Explanation

I investigated file access by opening a file through a file descriptor and observing how the shell exposes it through the process table. I also tested standard output, standard error, and combined redirection to show how Linux separates and captures stream data during command execution.
